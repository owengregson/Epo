{
  "name": "peanut-instagram-bot",
  "version": "3.0.0",
  "description": "Reliable, safe, observable Instagram growth engine built on a local event-sourced knowledge graph.",
  "main": "dist/main/main.js",
  "type": "commonjs",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:dev": "node esbuild.config.mjs --dev",
    "rebuild:electron": "electron-rebuild -f -w better-sqlite3",
    "rebuild:node": "npm rebuild better-sqlite3",
    "fix:electron": "node scripts/fix-electron-macos.mjs",
    "start": "npm run fix:electron && npm run rebuild:electron && npm run build && electron dist/main/main.js",
    "dev": "npm run fix:electron && npm run rebuild:electron && npm run build:dev && electron dist/main/main.js",
    "capture": "npm run fix:electron && npm run rebuild:electron && npm run build && electron dist/capture/capture-main.js",
    "livetest": "npm run fix:electron && npm run rebuild:electron && npm run build && electron dist/livetest/livetest-main.js",
    "inspect": "npm run fix:electron && npm run rebuild:electron && npm run build && electron dist/inspect/inspect-main.js",
    "test": "npm run rebuild:node && jest",
    "lint": "eslint src --ext .ts,.tsx"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "electron": "^43.4.0",
    "preact": "^10.28.3"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.6.0",
    "@types/better-sqlite3": "^7.6.11",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/parser": "^7.18.0",
    "esbuild": "^0.20.0",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.0"
  },
  "license": "MIT"
}
