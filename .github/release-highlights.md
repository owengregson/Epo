<!-- v3.0.3 -->

- Card spacing is back to normal: the first Settings card and the prune
  census card sat flush against their neighbors after the intro-tour
  update.
- The release pipeline is notarization-ready: the moment Apple Developer
  credentials are added as repository secrets, macOS builds sign and
  notarize automatically and first launch loses its warning dialog.
